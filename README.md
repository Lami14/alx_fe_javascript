# DOM Manipulation Project

This project demonstrates basic DOM manipulation using vanilla JavaScript.  
It focuses on interacting with HTML elements, handling events, and dynamically updating content on a webpage.

---

## 📂 Project Structure

```text
dom-manipulation/
│── index.html   # HTML structure
│── style.css    # Styling
│── script.js    # JavaScript DOM logic
